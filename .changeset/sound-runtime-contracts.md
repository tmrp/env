---
"@tmrp/env": minor
---

Make skipped-validation and client-filtered return types reflect their runtime
values, isolate automatic runtime detection from malformed unrelated globals,
and ignore inherited properties in explicit environment records.
