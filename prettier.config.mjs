/**
 * @see https://prettier.io/docs/configuration
 * @type {import("prettier").Config}
 */
const config = {
  printWidth: 80,
  proseWrap: "always",
  trailingComma: "es5",
  tabWidth: 2,
  semi: true,
  singleQuote: false,
};

export default config;
